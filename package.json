{
  "name": "metronome-be",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open  --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --ui",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.5",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vitest": "^0.25.5"
  }
}